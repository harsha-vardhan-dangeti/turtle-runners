node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Supabase CLI local state. `.temp/start-secrets/` holds the local stack's
# service-role key, JWT secret and database URL — never commit it.
supabase/.temp
supabase/.branches
